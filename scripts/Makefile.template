_all: $(TARGET)

include $(TOPDIR)/scripts/toolchain.mk

TARGET :=
OBJS := $($(wildcard *.c):.c=.o)

$(TARGET): $(OBJS)
	$(SILENT)$(CC) $(CFLAGS) -o $@ $^

clean:
	-@rm -f $(TARGET) *.d *.o *.a *.so

distclean: clean
	-@rm -f tags cscope.*

-include $(wildcard *.d)

